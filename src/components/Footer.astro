---
// The site footer, verbatim from the live site. It was byte-identical on all 15
// Nicepage pages, so there is one copy and no per-page variation to model.
import footerHtml from '../html/_footer.html?raw';
---

<Fragment set:html={footerHtml} />
